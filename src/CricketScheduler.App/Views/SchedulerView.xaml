<UserControl x:Class="CricketScheduler.App.Views.SchedulerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Action toolbar -->
        <DockPanel Grid.Row="0" LastChildFill="False">
            <TextBlock DockPanel.Dock="Left" Text="Schedule"
                       Style="{StaticResource SectionHeader}" VerticalAlignment="Center" Margin="0,0,16,0"/>
            <Button DockPanel.Dock="Left" Content="Generate Schedule"
                    Command="{Binding GenerateScheduleCommand}"
                    FontWeight="SemiBold" Padding="14,7" Margin="0,0,8,0"/>
            <Button DockPanel.Dock="Left" Content="Export CSV"
                    Command="{Binding ExportScheduleCommand}"
                    Padding="12,7" Margin="0,0,8,0"/>
            <Button DockPanel.Dock="Left" Content="Import Matches"
                    Command="{Binding ImportMatchesCommand}"
                    Padding="12,7" Margin="0,0,8,0"/>
            <TextBlock DockPanel.Dock="Right" Text="{Binding ScheduleStats}"
                       Foreground="#555" FontStyle="Italic" VerticalAlignment="Center"/>
        </DockPanel>

        <!-- Filter bar -->
        <Border Grid.Row="2" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6"
                Padding="12,8" Background="#FAFAFA">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Filters:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,0"/>

                <TextBlock Text="Division:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <ComboBox Width="160" ItemsSource="{Binding FilterDivisions}"
                          SelectedItem="{Binding FilterDivision}" Margin="0,0,14,0"/>

                <TextBlock Text="Team:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <ComboBox Width="190" ItemsSource="{Binding FilterTeams}"
                          SelectedItem="{Binding FilterTeam}" Margin="0,0,14,0"/>

                <TextBlock Text="Ground:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <ComboBox Width="160" ItemsSource="{Binding FilterGrounds}"
                          SelectedItem="{Binding FilterGround}" Margin="0,0,14,0"/>

                <Button Content="Clear Filters" Command="{Binding ClearFiltersCommand}" Padding="10,4"/>
            </StackPanel>
        </Border>

        <!-- Schedule DataGrid -->
        <DataGrid Grid.Row="4"
                  ItemsSource="{Binding FilteredScheduledMatches}"
                  SelectedItem="{Binding SelectedMatch}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  CanUserResizeColumns="True"
                  EnableRowVirtualization="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#"        Binding="{Binding Sequence}"    Width="45"/>
                <DataGridTextColumn Header="Division" Binding="{Binding DivisionName}" Width="120"/>
                <DataGridTextColumn Header="Date"     Binding="{Binding DateDisplay}"  Width="110"/>
                <DataGridTextColumn Header="Time"     Binding="{Binding TimeRange}"    Width="145"/>
                <DataGridTextColumn Header="Team One" Binding="{Binding TeamOne}"      Width="*"/>
                <DataGridTextColumn Header="Team Two" Binding="{Binding TeamTwo}"      Width="*"/>
                <DataGridTextColumn Header="Ground"   Binding="{Binding GroundName}"   Width="130"/>
                <DataGridTextColumn Header="Umpire 1" Binding="{Binding UmpireOne}"    Width="130"/>
                <DataGridTextColumn Header="Umpire 2" Binding="{Binding UmpireTwo}"    Width="130"/>
                <DataGridCheckBoxColumn Header="Fixed" Binding="{Binding IsFixed}"     Width="55"/>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasConflict}" Value="True">
                            <Setter Property="Background" Value="#FFEEEE"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsFixed}" Value="True">
                            <Setter Property="Background" Value="#E8F5E9"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
    </Grid>
</UserControl>
